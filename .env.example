# OpenClaw handles these secrets in its own config — you do NOT need this file
# for the plugin to work. This is just a reference for what secrets exist.

# Anthropic API key (configured in openclaw.json → agent.model)
# ANTHROPIC_API_KEY=sk-ant-...

# Telegram bot token (configured in OpenClaw channel setup)
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# Oura access (configured via OuraClaw plugin setup wizard)
# OURA_CLIENT_ID=...
# OURA_CLIENT_SECRET=...

# WellnessClaw dashboard password (optional, set in openclaw.json → plugins.entries.wellness-claw.config)
# WELLNESS_DASHBOARD_PASSWORD=your-password-here
